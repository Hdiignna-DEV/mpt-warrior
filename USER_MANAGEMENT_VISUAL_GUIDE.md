# 🎨 USER MANAGEMENT SYSTEM - VISUAL GUIDE

## 🏗️ System Architecture Diagram

```
┌─────────────────────────────────────────────────────────────────┐
│                      CLIENT (Browser)                            │
│                                                                  │
│  ┌───────────────┐  ┌───────────────┐  ┌──────────────────┐   │
│  │  Profile Page │  │  Admin HQ     │  │  Header Component│   │
│  │  /profile     │  │  /admin-hq/*  │  │  (ProfileHeader) │   │
│  └───────┬───────┘  └───────┬───────┘  └────────┬─────────┘   │
│          │                   │                    │              │
│          └───────────────────┴────────────────────┘              │
│                              │                                   │
│                    ┌─────────▼──────────┐                       │
│                    │  Zustand Store     │                       │
│                    │  (userProfileStore)│                       │
│                    │  - profile         │                       │
│                    │  - loadProfile()   │                       │
│                    │  - updateProfile() │                       │
│                    └─────────┬──────────┘                       │
│                              │                                   │
└──────────────────────────────┼───────────────────────────────────┘
                               │ JWT Token (Bearer)
                               │
┌──────────────────────────────▼───────────────────────────────────┐
│                      MIDDLEWARE LAYER                             │
│                                                                   │
│  ┌───────────────┐  ┌──────────────────┐  ┌──────────────────┐ │
│  │ requireAuth() │  │ requireAdmin()    │  │requireSuperAdmin()││
│  │ - Verify JWT  │  │ - Check role     │  │ - SUPER_ADMIN    │ │
│  │ - Attach user │  │ - ADMIN+         │  │   only           │ │
│  └───────┬───────┘  └────────┬─────────┘  └────────┬─────────┘ │
│          │                   │                      │            │
└──────────┼───────────────────┼──────────────────────┼────────────┘
           │                   │                      │
           │                   │                      │
┌──────────▼───────────────────▼──────────────────────▼────────────┐
│                         API ROUTES                                │
│                                                                   │
│  ┌─────────────────────────────────────────────────────────────┐│
│  │ USER ROUTES                                                  ││
│  │ GET  /api/profile              requireAuth                   ││
│  │ PUT  /api/profile/update       requireAuth                   ││
│  │ POST /api/referral/generate    requireAuth + VETERAN check   ││
│  └─────────────────────────────────────────────────────────────┘│
│                                                                   │
│  ┌─────────────────────────────────────────────────────────────┐│
│  │ ADMIN ROUTES                                                 ││
│  │ GET  /api/admin/users          requireAdmin                  ││
│  │ POST /api/admin/approve-user   requireAdmin                  ││
│  │ POST /api/admin/suspend-user   requireAdmin                  ││
│  └─────────────────────────────────────────────────────────────┘│
│                                                                   │
│  ┌─────────────────────────────────────────────────────────────┐│
│  │ SUPER ADMIN ROUTES                                           ││
│  │ GET  /api/admin/settings       requireSuperAdmin             ││
│  │ PUT  /api/admin/settings       requireSuperAdmin             ││
│  └─────────────────────────────────────────────────────────────┘│
│                                                                   │
└───────────────────────────────┬───────────────────────────────────┘
                                │
                                │ Cosmos DB SDK
                                │
┌───────────────────────────────▼───────────────────────────────────┐
│                    AZURE COSMOS DB                                 │
│                                                                    │
│  ┌──────────────────────┐  ┌──────────────────────┐              │
│  │  Container: users    │  │ Container: settings  │              │
│  │  Partition: /id      │  │ Partition: /id       │              │
│  │                      │  │                      │              │
│  │  - User profiles     │  │ - System config     │              │
│  │  - Badge levels      │  │ - Referral %        │              │
│  │  - Referral codes    │  │ - Badge thresholds  │              │
│  │  - Stats             │  │ - Feature flags     │              │
│  └──────────────────────┘  └──────────────────────┘              │
│                                                                    │
└────────────────────────────────────────────────────────────────────┘
```

---

## 👥 User Role Flow

```
┌─────────────────────────────────────────────────────────────┐
│                     NEW USER REGISTRATION                     │
└─────────────────────┬───────────────────────────────────────┘
                      │
                      ▼
              ┌───────────────┐
              │  Has Invite   │
              │     Code?     │
              └───────┬───────┘
                      │
          ┌───────────┴───────────┐
          │ YES                   │ NO
          ▼                       ▼
    ┌──────────┐           ┌────────────┐
    │ Register │           │  Rejected  │
    │ Success  │           └────────────┘
    └─────┬────┘
          │
          ▼
    ┌──────────────┐
    │ Role: PENDING│
    │ Status: pend │
    └──────┬───────┘
           │
           ▼
    ┌──────────────────┐
    │  Admin Review    │
    └──────┬───────────┘
           │
    ┌──────┴──────┐
    │             │
    ▼             ▼
┌─────────┐  ┌─────────┐
│ APPROVE │  │ REJECT  │
└────┬────┘  └────┬────┘
     │            │
     ▼            ▼
┌──────────┐  ┌─────────────┐
│  WARRIOR │  │ Status:     │
│  ACTIVE  │  │ rejected    │
└────┬─────┘  └─────────────┘
     │
     │ Progress through system
     ▼
┌────────────────────────────┐
│   BADGE PROGRESSION        │
│                            │
│  RECRUIT (0-49 trades)     │
│     ↓                      │
│  WARRIOR (50-99 trades)    │
│     ↓                      │
│  VETERAN (100+ trades      │
│           + 750+ disc.)    │
└────────────────────────────┘
```

---

## 🎖️ Badge Level Requirements

```
┌────────────────────────────────────────────────────────────┐
│                     BADGE PROGRESSION                       │
└────────────────────────────────────────────────────────────┘

🥉 RECRUIT
┌───────────────────────────────────────────────────┐
│ Requirements:                                     │
│ ✓ Account created                                │
│ ✓ Onboarding completed                           │
│                                                   │
│ Access:                                          │
│ • Dashboard                                      │
│ • Trading Journal                                │
│ • Risk Calculator                                │
│ • AI Mentor                                      │
│ • Academy                                        │
│                                                   │
│ Cannot:                                          │
│ ✗ Generate referral codes                       │
└───────────────────────────────────────────────────┘
                    ↓
            (50+ trades completed)
                    ↓
🥈 WARRIOR
┌───────────────────────────────────────────────────┐
│ Requirements:                                     │
│ ✓ 50+ total trades                              │
│ ✓ Basic discipline score (300+)                 │
│                                                   │
│ New Access:                                      │
│ • Advanced analytics                             │
│ • Badge showcase                                 │
│ • Community features                             │
│                                                   │
│ Still Cannot:                                    │
│ ✗ Generate referral codes                       │
└───────────────────────────────────────────────────┘
                    ↓
      (100+ trades + 750+ discipline score)
                    ↓
🥇 VETERAN
┌───────────────────────────────────────────────────┐
│ Requirements:                                     │
│ ✓ 100+ total trades                             │
│ ✓ 750+ discipline score                         │
│ ✓ 60%+ win rate                                 │
│                                                   │
│ Elite Access:                                    │
│ ✅ Generate LEGACY referral codes               │
│ ✅ Earn referral commissions                    │
│ ✅ Mentor new recruits                          │
│ ✅ VIP badge display                            │
│ ✅ Priority support                             │
└───────────────────────────────────────────────────┘
```

---

## 🔐 Authorization Flow

```
┌──────────────────────────────────────────────────┐
│           API REQUEST WITH JWT TOKEN             │
└───────────────────┬──────────────────────────────┘
                    │
                    ▼
            ┌───────────────┐
            │ Verify Token  │
            │ (middleware)  │
            └───────┬───────┘
                    │
        ┌───────────┴───────────┐
        │ Valid?                │
        └───┬───────────────┬───┘
            │ YES           │ NO
            ▼               ▼
    ┌───────────┐    ┌──────────┐
    │ Extract   │    │ Return   │
    │ User Data │    │ 401      │
    └─────┬─────┘    └──────────┘
          │
          ▼
    ┌─────────────┐
    │ Check Role  │
    │ Required    │
    └──────┬──────┘
           │
    ┌──────┴──────┐
    │             │
    ▼             ▼
┌─────────┐  ┌──────────┐
│ WARRIOR │  │ ADMIN+   │
│ Routes  │  │ Routes   │
└────┬────┘  └─────┬────┘
     │             │
     ▼             ▼
┌─────────┐  ┌──────────────┐
│ Allow   │  │ Check Admin  │
│ Access  │  │ or Super?    │
└─────────┘  └──────┬───────┘
                    │
             ┌──────┴──────┐
             │ YES         │ NO
             ▼             ▼
        ┌─────────┐  ┌──────────┐
        │ Allow   │  │ Return   │
        │ Access  │  │ 403      │
        └─────────┘  └──────────┘
```

---

## 💾 Data Flow Example

### Example: User Views Their Profile

```
1. User clicks "Profile" button
   ↓
2. Browser checks Zustand store
   ↓
3. If profile exists in store → Show immediately
   If not → Fetch from API
   ↓
4. API: GET /api/profile
   Headers: { Authorization: "Bearer <JWT>" }
   ↓
5. Middleware: requireAuth()
   - Verify JWT token
   - Extract user ID from token
   ↓
6. API Handler:
   - Connect to Cosmos DB
   - Query: SELECT * FROM users WHERE id = userId
   - Return profile data
   ↓
7. Client receives response:
   {
     success: true,
     profile: {
       id: "...",
       warriorId: "MPT-2026-00001",
       name: "John Warrior",
       currentBadgeLevel: "WARRIOR",
       stats: { ... }
     }
   }
   ↓
8. Zustand store updates
   ↓
9. All components re-render with new data
   - ProfileHeader shows avatar
   - Dashboard shows stats
   - Badge display updates
```

---

## 🔄 State Management Flow

```
┌──────────────────────────────────────────────────┐
│             ZUSTAND STORE (CLIENT)               │
│                                                  │
│  State:                                         │
│  ┌────────────────────────────────────────┐    │
│  │ profile: {                             │    │
│  │   id: "user-123",                      │    │
│  │   warriorId: "MPT-2026-00001",        │    │
│  │   currentBadgeLevel: "WARRIOR",       │    │
│  │   stats: { ... }                      │    │
│  │ }                                      │    │
│  └────────────────────────────────────────┘    │
│                                                  │
│  Actions:                                       │
│  • setProfile(profile)                          │
│  • updateProfile(updates)                       │
│  • loadProfile()          → API Call           │
│  • refreshProfile()       → API Call           │
│  • clearProfile()         → Logout             │
│                                                  │
│  Persistence:                                   │
│  localStorage: "mpt-user-profile"              │
└────────────────┬─────────────────────────────────┘
                 │
                 │ Used by:
                 │
    ┌────────────┼────────────┐
    │            │            │
    ▼            ▼            ▼
┌─────────┐ ┌──────────┐ ┌──────────┐
│ Header  │ │Dashboard │ │ Profile  │
│Component│ │  Page    │ │   Page   │
└─────────┘ └──────────┘ └──────────┘

All components auto-update when store changes!
```

---

## 🎯 Component Hierarchy

```
App Root
│
├── Layout
│   ├── Header
│   │   ├── MptLogo
│   │   ├── Navigation
│   │   └── ProfileHeader ← Zustand store
│   │       ├── Avatar (from profile.avatar)
│   │       ├── Name (from profile.displayName)
│   │       ├── Warrior ID (from profile.warriorId)
│   │       └── BadgeLevelDisplay (from profile.currentBadgeLevel)
│   │
│   ├── Sidebar
│   │   ├── Navigation Links
│   │   └── ProfileBadge ← Zustand store
│   │
│   └── Footer
│
├── Pages
│   │
│   ├── /dashboard
│   │   ├── Stats Cards
│   │   ├── ProfileStatsCard ← Zustand store
│   │   └── Quick Actions
│   │
│   ├── /profile
│   │   ├── Profile Header (avatar + stats)
│   │   ├── Stats Grid (win rate, trades, etc.)
│   │   ├── Badge Collection
│   │   ├── Personal Goal
│   │   └── Referral Section (if VETERAN)
│   │
│   ├── /profile/edit
│   │   ├── Avatar Upload
│   │   ├── Basic Info Form
│   │   ├── Trading Profile Form
│   │   └── Privacy Settings
│   │
│   └── /admin-hq (ADMIN+ only)
│       ├── /users
│       │   ├── Stats Cards
│       │   ├── Filter Panel
│       │   └── User Table
│       │       ├── User Row
│       │       │   ├── User Info
│       │       │   ├── Role Badge
│       │       │   ├── Status Badge
│       │       │   └── Action Buttons
│       │       └── ...
│       │
│       └── /settings (SUPER_ADMIN only)
│           ├── Referral Settings
│           ├── Badge Progression
│           └── System Controls
│
└── Global Components
    ├── ProfileHeader (reusable)
    ├── ProfileBadge (reusable)
    ├── ProfileStatsCard (reusable)
    └── BadgeLevelDisplay (reusable)
```

---

## 📱 Responsive Breakpoints

```
Mobile (< 768px)
┌────────────────┐
│    Header      │
│  [☰] [Logo] [👤]│
├────────────────┤
│                │
│   Main Content │
│   (Full Width) │
│                │
├────────────────┤
│ Bottom Nav Bar │
└────────────────┘

Tablet (768px - 1024px)
┌─────────────────────┐
│      Header         │
│ [Logo] [Nav] [👤]   │
├─────────────────────┤
│                     │
│   Main Content      │
│   (Centered)        │
│                     │
└─────────────────────┘

Desktop (> 1024px)
┌───┬─────────────────────┬───┐
│ S │      Header         │   │
│ i │ [Logo] [Nav] [👤]   │   │
│ d ├─────────────────────┤   │
│ e │                     │ W │
│ b │   Main Content      │ i │
│ a │   (Max Width)       │ d │
│ r │                     │ g │
│   │                     │ e │
└───┴─────────────────────┴───┘
```

---

**Created for MPT Warrior Team**  
**Visual Guide v1.0 - January 7, 2026**
